#include <bits/stdc++.h>
/*
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--){
        long long n, a, b;
        cin>> n >> a >> b;

        long long ans = 0, cnt = 1;
        while (cnt <= n){
            if ((n-cnt)%b == 0){
                ans = 1; break;
            }
            cnt *= a;
            if (a == 1) break;
        }

        if (ans) cout << "YES\n";
        else cout << "NO\n";

    }
    return 0;
}
*/