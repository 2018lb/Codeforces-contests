#include <bits/stdc++.h>
using namespace std;
/*

int main() {
    int t;
    cin >> t;
    while(t--){
        long long a, b;
        cin >> a >> b;
        long long k = abs(a-b);
        if (k == 0) cout << "0 0\n";
        else cout << k << ' ' << min(a%k, k-a%k) << endl;
    }
    return 0;
}
*/