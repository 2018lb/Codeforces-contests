#include <bits/stdc++.h>
using namespace std;
/*


int main() {
    ios::sync_with_stdio(false);
    int t;
    cin >> t;
    while (t--){
       long long n;
       cin >> n;
       if (n%2050!=0){
           cout << -1 << endl;
           continue;
       }

       long long ans = n / 2050, tot = 0;
       while (ans){
           tot += ans %10;
           ans /= 10;
       }
       cout << tot << endl;
    }

    return 0;
}
*/