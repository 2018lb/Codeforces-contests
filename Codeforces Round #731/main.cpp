#include <bits/stdc++.h>
using namespace std;
/*
struct sd{
    int x, y;
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--){
        sd a, b, f;
        cin >> a.x >> a.y >> b.x >> b.y >> f.x >> f.y;
        int ans = abs(a.x-b.x)+abs(a.y-b.y);
        if (a.x==b.x && b.x==f.x && ((f.y < a.y&&f.y > b.y)||(f.y > a.y&&f.y < b.y))) ans += 2;
        if (a.y==b.y && b.y==f.y && ((f.x < a.x&&f.x > b.x)||(f.x > a.x&&f.x < b.x))) ans += 2;

        cout << ans << endl;
    }
    return 0;
}*/
