#include <bits/stdc++.h>

using namespace std;
/*
const int N = 5e5+100;

map <double ,int > q;
char s[N];


int main(void){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while(t--){
        q.clear();
        int n;
        cin >> n;
        cin >> s;
        double d = 0, k = 0;
        for (int i = 0; i < n; i++){
            if (s[i] == 'D') d++;
            if (s[i] == 'K') k++;
            q[d/k]++;
            cout << q[d/k] << ' ';
        }
        cout << endl;
    }
}
*/