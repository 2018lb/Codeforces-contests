#include <bits/stdc++.h>
/*
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while(t--){
        int n, sum = 0;
        cin>> n;
        for (int i = 1; i <= n; i++){
            int x;
            cin >> x;
            sum += x;
        }
        if (sum == n) {
            cout << "0\n";
            continue;
        }
        if (sum < n) {
            cout << "1\n";
            continue;
        }
        cout << sum - n << endl;
    }
    return 0;
}
*/